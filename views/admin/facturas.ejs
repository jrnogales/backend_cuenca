<% layout('partials/layout') %>

<a href="/admin" class="btn">← Volver</a>
<h2>🧾 Facturas</h2>

<div class="card">
  <table class="table">
    <thead>
      <tr>
        <th>#</th><th>Código</th><th>Reserva</th><th>Fecha</th>
        <th>Subtotal</th><th>IVA</th><th>Total</th><th>Método</th><th>Estado</th><th></th>
      </tr>
    </thead>
    <tbody>
    <% facturas.forEach(f => { %>
      <tr>
        <td><%= f.id %></td>
        <td><%= f.codigo_factura %></td>
        <td><%= f.codigo_reserva || '-' %></td>
        <td><%= f.fecha_emision %></td>
        <td>$<%= Number(f.subtotal).toFixed(2) %></td>
        <td>$<%= Number(f.iva).toFixed(2) %></td>
        <td><b>$<%= Number(f.total).toFixed(2) %></b></td>
        <td><%= f.metodo_pago %></td>
        <td><span class="badge"><%= f.estado %></span></td>
        <td><a class="btn-primary" href="/admin/facturas/<%= f.id %>">Ver</a></td>
      </tr>
    <% }) %>
    </tbody>
  </table>
</div>
